## Packages
framer-motion | Essential for the premium reveal and scroll animations requested in the design

## Notes
- The application uses `framer-motion` to replicate the smooth fade-up and floating animations from the original HTML template.
- Assessment form submissions go to `POST /api/assessments`.
- The design relies heavily on custom CSS variables mapped to the requested color palette (`--cream`, `--sage`, etc.).
